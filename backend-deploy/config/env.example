# MongoDB数据库连接配置示例
# 创建 .env 文件并复制以下内容，然后修改为你的实际配置

# MongoDB连接配置
# 远程MongoDB（默认）
MONGODB_URI=mongodb://admin:pwd123@119.45.15.167:27017/maintenance_system?authSource=maintenance_system
MONGODB_DB=maintenance_system

# 本地MongoDB示例:
# MONGODB_URI=mongodb://localhost:27017

# 如果使用MongoDB Atlas云数据库，请使用以下格式:
# MONGODB_URI=mongodb+srv://username:password@cluster0.abcde.mongodb.net/maintenance_system?retryWrites=true&w=majority

# 其他认证示例:
# MONGODB_URI=mongodb://username:password@localhost:27017/maintenance_system?authSource=admin

# 如果使用自定义端口:
# MONGODB_URI=mongodb://localhost:27018

# 如果使用远程MongoDB服务器:
# MONGODB_URI=mongodb://192.168.1.100:27017

# 其他数据库名称示例:
# MONGODB_DB=my_custom_database

# MySQL数据库配置 (如果需要)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=maintenance_system

# API服务配置
API_PORT=3001

# 其他配置
VUE_APP_API_URL=http://localhost:3001
